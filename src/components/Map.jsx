import { View } from 'react-native'
import { Text } from './theme/Text';

export const Map = () => {
    return (
        <View style={{backgroundColor: "#fff", flex: 1}}>
            <Text>That is Map view</Text>
        </View>
    ); 
}